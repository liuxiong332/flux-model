{
  "name": "flux-model",
  "version": "1.0.0",
  "description": "The flux-like data flow container.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^1.8.10"
  }
}
