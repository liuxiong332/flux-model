{
  "name": "flux-model",
  "version": "1.0.0",
  "description": "The flux-like data flow container.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "immutable": "^3.8.1",
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  },
  "peerDependencies": {
    "immutable": ">=3"
  }
}
